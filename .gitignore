# Personal dev scratch space
dev/

# Rust build artifacts
internal/target/
target/
*.rs.bk

# macOS
.DS_Store
**/.DS_Store

# Env files (real ones — .env.example is tracked)
.env
.env.local
.env.*.local

# Playwright
**/playwright-report/
**/test-results/

# FUSE filesystem temp files (Linux file manager / gvfs)
.fuse_hidden*

# Editor
*.swp
*.swo

# Agent / AI-tool artifacts — never commit (zero AI trace).
.claude/
.agents/
.cursor/
.aider*
.continue/
.playwright-mcp/
**/*-mcp/
AGENTS.md
CLAUDE.md
GEMINI.md
